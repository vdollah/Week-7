Week 7

For this week I worked on Module 4, which was about debugging and using if statements the right way. The first part was making a small menu that lets the user pick an option. The program checks the choice with if and else if, and then prints what the user picked. The goal was to make sure the program ran with no errors and that the choices worked the way they should.

The second part was fixing a broken program that was supposed to find the biggest number out of three inputs. The code had mistakes, so I had to fix it and also change it so it used nested if statements. After fixing it, the program reads three numbers and then shows which one is the largest. Everything runs the right way and the output is correct.
